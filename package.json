{
  "name": "isee-thesis-agent",
  "version": "1.0.0",
  "description": "Multi-agent AI system for generating Master's Thesis chapters on the I.S.E.E. flood monitoring platform.",
  "private": true,
  "scripts": {
    "dev:api": "node -e \"require('./api/generate.js')\" && echo API loaded",
    "dev:frontend": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "vercel-build": "npm run build -w frontend"
  },
  "workspaces": [
    "frontend"
  ],
  "dependencies": {
    "@google/genai": "^1.3.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}